# Scrapers Worker Environment Variables
# These are set via `wrangler secret put <NAME>` for deployment
# For local development, create a .dev.vars file with these values

# PostgreSQL Database Connection
# Get a free database at https://neon.tech (recommended) or https://supabase.com
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Google Gemini API Key (FREE tier: 15 RPM, 1M tokens/day)
# Get your key at https://aistudio.google.com/apikey
GOOGLE_API_KEY="AIzaSy..."
GOOGLE_BASE_URL="https://generativelanguage.googleapis.com/v1beta/openai/"

# API Authentication Secret
# Generate with: openssl rand -hex 32
FORESIGHTAI_SECRET_KEY="your-random-secret-key"

# === OPTIONAL: Paid Features ===
# Browser Rendering (costs ~$0.02/1000 requests)
# Only enable if you need to scrape paywalled sites
# ENABLE_BROWSER_RENDERING="true"
# CLOUDFLARE_ACCOUNT_ID="your-account-id"
# CLOUDFLARE_BROWSER_RENDERING_API_TOKEN="your-token"
